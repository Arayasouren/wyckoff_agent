# ── Core (required) ───────────────────────────────────────────────────────
anthropic>=0.40.0
aiosqlite>=0.20.0
python-dotenv>=1.0.0
rich>=13.7.0
numpy>=1.24
pandas>=2.0
requests>=2.28          # talks to the Wyckoff service (data + analysis)

# ── Optional ──────────────────────────────────────────────────────────────
openai>=1.40            # OpenAI-compatible LLM provider + embeddings
# matplotlib>=3.7       # only needed when FINAGENT_ENABLE_CHART=1
